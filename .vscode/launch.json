{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "运行当前 JS 文件",
      "program": "${file}",
      "cwd": "${fileDirname}",
      "console": "integratedTerminal",
      "outputCapture": "std",
      "env": {
        "NODE_OPTIONS": ""
      }
    }
  ]
}