{
  "name": "QueryTable",
  "summary": "<p><strong>QueryTable</strong> 对象是 <strong>QueryTables</strong> 集合的成员。</p>\n<p><b class=\"mainheaders\">示例</b></p><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例开启对第一张工作表上新增查询表的列宽自动调整，并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\示例\\\\test1.accdb;DefaultDir=D:\\\\示例\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">AdjustColumnWidth</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示工作表 Sheet1 上包含 B1 单元格的查询表的查询类型是否为 xlOLEDBQuery。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">QueryType</span> == xlOLEDBQuery)\n}</code></pre></td></tr></tbody></table></div>",
  "description": "<p>代表一个利用从外部数据源（如 SQL Server 或 Access 数据库）返回的数据生成的工作表表格。</p>\n",
  "category": "et",
  "properties": [
    {
      "name": "AdjustColumnWidth",
      "summary": "<p>最大列宽为屏幕宽度的三分之二。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>AdjustColumnWidth</strong> 属性。</p>\n",
      "description": "<p>如果每次刷新指定的查询表时列宽都会自动调整为最适合的宽度，则为 <strong>True</strong>。如果每次刷新时列宽不进行自动调整，则为 <strong>False</strong>。默认值为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例关闭对新增查询表（第一个工作簿中的第一张工作表上）的列宽自动调整。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\test1.accdb;DefaultDir=D:\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">AdjustColumnWidth</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例显示工作表 Sheet1 上第一张查询表的列宽是否自动调整。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">AdjustColumnWidth</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "Application",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>Application</strong> 属性。</p>\n",
      "description": "<p>如果不与对象识别符一起使用，则此属性返回代表 ET 应用程序的 <strong>Application</strong>对象。如果与对象识别符一起使用，则此属性返回代表指定对象的创建者的 <strong>Application</strong> 对象（可对 OLE 自动操作对象使用此属性来返回该对象的应用程序）。只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">//本示例显示一条有关创建 myObject 的应用程序的消息。</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>)\n{\n    <span class=\"hljs-keyword\">let</span> myObject = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveWorkbook</span>\n    <span class=\"hljs-keyword\">if</span>(myObject.<span class=\"hljs-property\">Application</span>.<span class=\"hljs-property\">Value</span> == <span class=\"hljs-string\">&quot;ET&quot;</span>) \n    {\n\t<span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">&quot;This is an ET Application object.&quot;</span>)\n    }\n    <span class=\"hljs-keyword\">else</span> \n    {\n\t<span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">&quot;This is not an ET Application object.&quot;</span>)\n    }\n}</code></pre></td></tr></tbody></table></div>",
      "type": "Application",
      "readonly": true
    },
    {
      "name": "BackgroundQuery",
      "summary": "<p>对于 OLAP（OLAP：为查询和报表（而不是处理事务）而进行了优化的数据库技术。OLAP 数据是按分级结构组织的，它存储在多维数据集而不是表中。） 数据源，此属性是只读的，并且始终返回 <strong>False</strong>。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>BackgroundQuery</strong> 属性。</p>\n",
      "description": "<p>如果查询表的查询是异步执行（在后台执行）的，则为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例将活动工作表上新增查询表的查询设置为异步执行。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;OLEDB;Provider=Microsoft.ACE.OLEDB.12.0;User ID=Admin;Data Source=D:\\\\example1.xlsx;Mode=Share Deny Write;Extended Properties=&#x27;HDR=YES&#x27;;Jet OLEDB:Engine Type=37;Jet OLEDB:Database Locking Mode=0;Jet OLEDB:Global Partial Bulk Ops=2;Jet OLEDB:Global Bulk Transactions=1;Jet OLEDB:Create System Database=False;Jet OLEDB:Encrypt Database=False;Jet OLEDB:Don&#x27;t Copy Locale on Compact=False;Jet OLEDB:Compact Without Replica Repair=False;Jet OLEDB:SFP=False;Jet OLEDB:Support Complex Data=False;Jet OLEDB:Bypass UserInfo Validation=False;Jet OLEDB:Limited DB Caching=False;Jet OLEDB:Bypass ChoiceField Validation=False&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `Sheet1$`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">BackgroundQuery</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例显示第一张工作表上第一张查询表的查询是否为异步执行。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">BackgroundQuery</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "CommandText",
      "summary": "<p>对于 OLE DB 源，<strong>CommandType</strong> 属性描述 <strong>CommandText</strong> 属性的值。</p>\n<p>对于 ODBC 源，设置 <strong>CommandText</strong> 将导致刷新数据。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>CommandText</strong> 属性。</p>\n<p>包含相应查询表的工作表必须为活动状态，才可访问此属性。</p>\n",
      "description": "<p>返回或设置指定数据源的命令串。<strong>Variant</strong> 型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例设置第一张工作簿上第一张工作表中第一张查询表的 ODBC 数据源的命令串。注意，该命令串是一个 SQL 语句。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">CommandType</span> = xlCmdSql\n    qyTable.<span class=\"hljs-property\">CommandText</span> = <span class=\"hljs-string\">&quot;SELECT * FROM `student` WHERE `age` &lt; &#x27;12&#x27; &quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表中第一张查询表的命令串。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">CommandText</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "any",
      "readonly": false
    },
    {
      "name": "CommandType",
      "summary": "<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlCmdType</strong> 可为以下这些 <strong>XlCmdType</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlCmdCube</strong>。包含 OLAP（OLAP：为查询和报表（而不是处理事务）而进行了优化的数据库技术。OLAP 数据是按分级结构组织的，它存储在多维数据集而不是表中。） 数据源的多维数据集（多维数据集：一种 OLAP 数据结构。多维数据集包含维度，如“国家/地区/省（或市/自治区）/市（或县）”，还包括数据字段，如“销售额”。维度将各种类型的数据组织到带有明细数据级别的分层结构中，而数据字段度量数量。）名称。</td>\n</tr>\n<tr>\n<td><strong>xlCmdDefault</strong>。包含 OLE DB 提供者可识别的命令文本。</td>\n</tr>\n<tr>\n<td><strong>xlCmdSql</strong>。包含一个 SQL 语句。</td>\n</tr>\n<tr>\n<td><strong>xlCmdTable</strong>。包含用于访问 OLE DB 数据源的表名称。</td>\n</tr>\n</tbody></table></div><p>只有当查询表或数据透视表缓存的 <strong>QueryType</strong> 属性值为 <strong>xlOLEDBQuery</strong> 时，才可以设置 <strong>CommandType</strong> 属性。</p>\n<p>当 <strong>CommandType</strong> 属性的值为 <strong>xlCmdCube</strong> 时，如果没有与查询表相关联的数据透视表，则不能更改该值。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>CommandType</strong> 属性。</p>\n",
      "description": "<p>返回或设置备注部分的下表中列出的 <strong>XlCmdType</strong> 常量之一。返回或设置的常量用于描述 <strong>CommandText</strong> 属性的值。默认值为 <strong>xlCmdSQL</strong>。<strong>XlCmdType</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例设置第一张工作簿上第一张工作表中第一张查询表的 ODBC 数据源的命令串。注意，该命令串是一个 SQL 语句。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">CommandType</span> = xlCmdSql\n    qyTable.<span class=\"hljs-property\">CommandText</span> = <span class=\"hljs-string\">&quot;SELECT * FROM `student` WHERE `age` &lt; &#x27;14&#x27;&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表中第一张查询表的 CommandType 属性是否为 xlCmdSql。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">CommandType</span> == xlCmdSql)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlCmdType",
      "readonly": false
    },
    {
      "name": "Connection",
      "summary": "<p>设置 <strong>Connection</strong> 属性不会立即与数据源建立连接。必须使用 <strong>Refresh</strong> 方法建立连接并检索数据。</p>\n<p>有关连接字符串语法的详细信息，请参阅 <strong>QueryTables</strong> 集合的 <strong>Add</strong> 方法。</p>\n<p>另外，也可以通过选择 ActiveX 数据对象 (ADO) 库直接访问数据源。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>Connection</strong> 属性。</p>\n",
      "description": "<p>返回或设置包含下列某项的字符串：允许 ET 连接到 OLE DB 数据源的 OLE DB 设置；允许 ET 连接到 ODBC 数据源的 ODBC 设置；允许 ET 连接到 Web 数据源的 URL；或者文本文件的名称或路径，或是指定某个数据库或 Web 查询的文件名称或路径。<strong>Variant</strong> 型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例为第一张工作表上的查询表“ExternalData_1”提供新的 ODBC 连接信息，并设置其命令串。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;ExternalData_1&quot;</span>)\n    qyTable.<span class=\"hljs-property\">Connection</span> = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\test1.accdb;DefaultDir=D:\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    qyTable.<span class=\"hljs-property\">CommandText</span> = <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例设置工作表 Sheet1 上包含单元格 I1 的查询表的连接文件，并设置其命令串。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    qyTable.<span class=\"hljs-property\">Connection</span> = <span class=\"hljs-string\">&quot;TEXT;D:\\\\test3.txt&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "any",
      "readonly": false
    },
    {
      "name": "Creator",
      "summary": "<p>如果该对象是在 ET 中创建的，则此属性返回字符串 XCEL，它等同于十六进制的数字 5843454C。<strong>Creator</strong> 属性是为 Macintosh 上的 ET 设计的，在 Macintosh 上，每个应用程序都具有一个四字符的创建者代码。例如，ET 的创建者代码为 XCEL。  </p>\n<p>来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据则将作为 <strong>ListObject</strong>对象导入。您可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>Creator</strong> 属性。</p>\n",
      "description": "<p>返回一个 32 位整数，该整数指示在其中创建此对象的应用程序。只读 <strong>Long</strong> 类型。</p>\n",
      "examples": "",
      "type": "XlCreator",
      "readonly": true
    },
    {
      "name": "Destination",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>Destination</strong> 属性。</p>\n",
      "description": "<p>返回查询表目标区域（查询结果表放置的区域）的左上角单元格。目标区域必须位于包含 <strong>QueryTable</strong> 对象的工作表中。<strong>Range</strong> 类型，只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例滚动活动窗口，将第一张工作表上第一张查询表的左上角单元格滚动至窗口的左上角。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> destion = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Destination</span>\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveWindow</span>.<span class=\"hljs-property\">ScrollColumn</span> = destion.<span class=\"hljs-property\">Column</span>\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveWindow</span>.<span class=\"hljs-property\">ScrollRow</span> = destion.<span class=\"hljs-property\">Row</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上第一张查询表左上角单元格的地址。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Destination</span>.<span class=\"hljs-title class_\">Address</span>())\n}</code></pre></td></tr></tbody></table></div>",
      "type": "Range",
      "readonly": true
    },
    {
      "name": "EnableEditing",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>EnableEditing</strong> 属性。</p>\n",
      "description": "<p>如果允许用户对指定查询表进行编辑，则该值为 <strong>True</strong>；如果用户只能刷新查询表，则该值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对第一张工作表上第一张查询表进行设置，使用户不能对其进行编辑。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">EnableEditing</span> = <span class=\"hljs-literal\">false</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B1 单元格的查询表是否允许用户对该查询表进行编辑。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">EnableEditing</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "EnableRefresh",
      "summary": "<p>如果 <strong>EnableRefresh</strong> 属性设置为 <strong>False</strong>，则忽略 <strong>RefreshOnFileOpen</strong> 属性。</p>\n<p>对于 OLAP（OLAP：为查询和报表（而不是处理事务）而进行了优化的数据库技术。OLAP 数据是按分级结构组织的，它存储在多维数据集而不是表中。） 数据源，将此属性设置为 <strong>False</strong> 会禁用更新。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>EnableRefresh</strong> 属性。</p>\n",
      "description": "<p>如果用户可刷新数据透视表高速缓存或查询表，则为 <strong>True</strong>。默认值为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一张工作表上第一张查询表设置为（用户）不可刷新。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">EnableRefresh</span> = <span class=\"hljs-literal\">false</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B1 单元格的查询表（用户）是否可以刷新。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">EnableRefresh</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "FetchedRowOverflow",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>FetchedRowOverflow</strong> 属性。</p>\n",
      "description": "<p>如果上次使用 <strong>Refresh</strong> 方法返回的行数比工作表中可用行数大，则该值为 <strong>True</strong>。<strong>Boolean</strong> 类型，只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对第一张工作表上第一张查询表进行刷新。如果由查询返回的行数超过工作表中的可用行数，则显示一条错误消息。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> <span class=\"hljs-title class_\">Qtable</span> = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title class_\">Qtable</span>.<span class=\"hljs-title class_\">Refresh</span>()\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-title class_\">Qtable</span>.<span class=\"hljs-property\">FetchedRowOverflow</span>) {\n        <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">&quot;Query too large: please redefine.&quot;</span>)\n    }\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对活动工作表上包含 B2 单元格的查询表进行刷新。显示由查询返回的行数是否超过工作表中的可用行数。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qytable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B2&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    qytable.<span class=\"hljs-title class_\">Refresh</span>()\n    <span class=\"hljs-title function_\">alert</span>(qytable.<span class=\"hljs-property\">FetchedRowOverflow</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": true
    },
    {
      "name": "FieldNames",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>FieldNames</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果数据源的字段名称作为返回数据的列标题显示，则该值为 <strong>True</strong>。默认值为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对第一张工作表上第一张查询表进行设置，使表中不显示字段名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qytable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qytable.<span class=\"hljs-property\">FieldNames</span> = <span class=\"hljs-literal\">false</span>\n    qytable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B1 单元格的查询表是否展示字段名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">FieldNames</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "FillAdjacentFormulas",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>FillAdjacentFormulas</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果每当查询表刷新时，指定查询表右侧的公式就自动更新，则该属性值为 <strong>True</strong>。默认值为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对工作表 Sheet1 上第一张查询表进行设置，每当查询表刷新时就自动更新右侧的公式。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Sheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">FillAdjacentFormulas</span> = <span class=\"hljs-literal\">true</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B1 单元格的查询表刷新时，该查询表右侧的公式是否自动更新。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">FillAdjacentFormulas</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "ListObject",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。  </p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>ListObject</strong> 属性仅适用于 <strong>ListObject</strong> 对象。</p>\n",
      "description": "<p>为 <strong>QueryTable</strong> 对象返回一个 <strong>ListObject</strong> 对象。<strong>ListObject</strong> 对象类型，只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 A1 单元格的表格上查询表的ListObject的地址。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>).<span class=\"hljs-property\">ListObject</span>.<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">ListObject</span>.<span class=\"hljs-property\">Range</span>.<span class=\"hljs-title class_\">Address</span>())\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上第一张表格上查询表的ListObject当前源是否为xlSrcQuery。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">ListObjects</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">ListObject</span>.<span class=\"hljs-property\">SourceType</span> == xlSrcQuery)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "ListObject",
      "readonly": true
    },
    {
      "name": "MaintainConnection",
      "summary": "<p>只有当查询表或数据透视表缓存的 <strong>QueryType</strong> 属性值为 <strong>xlOLEDBQuery</strong> 时，才可以设置 <strong>MaintainConnection</strong> 属性。</p>\n<p>如果预计会频繁对服务器进行查询，则可将此属性设置为 <strong>True</strong>，这样能减少重新连接的时间因而可提高性能。将此属性设置为 <strong>False</strong>，将会关闭一个打开的连接。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>MaintainConnection</strong> 属性。</p>\n",
      "description": "<p>如果从刷新数据开始直至关闭工作簿，都一直保留指向指定数据源的连接，则为 <strong>True</strong>。默认值是 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例关闭活动工作表上新增查询表一直指向数据源的连接。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;OLEDB;Provider=Microsoft.ACE.OLEDB.12.0;User ID=Admin;Data Source=D:\\\\示例\\\\example1.xlsx;Mode=Share Deny Write;Extended Properties=&#x27;HDR=YES&#x27;;Jet OLEDB:Engine Type=37;Jet OLEDB:Database Locking Mode=0;Jet OLEDB:Global Partial Bulk Ops=2;Jet OLEDB:Global Bulk Transactions=1;Jet OLEDB:Create System Database=False;Jet OLEDB:Encrypt Database=False;Jet OLEDB:Don&#x27;t Copy Locale on Compact=False;Jet OLEDB:Compact Without Replica Repair=False;Jet OLEDB:SFP=False;Jet OLEDB:Support Complex Data=False;Jet OLEDB:Bypass UserInfo Validation=False;Jet OLEDB:Limited DB Caching=False;Jet OLEDB:Bypass ChoiceField Validation=False&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `Sheet1$`&quot;</span>)\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n    qyTable.<span class=\"hljs-property\">MaintainConnection</span> = <span class=\"hljs-literal\">false</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B1 单元格的查询表是否一直保留指向数据源的连接。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">MaintainConnection</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "Name",
      "summary": "<p>下表列出了 <strong>Name</strong> 属性和相关属性的示例值，同时假设有一个具有唯一名称“[Europe].[France].[Paris]”的 OLAP（OLAP：为查询和报表（而不是处理事务）而进行了优化的数据库技术。OLAP 数据是按分级结构组织的，它存储在多维数据集而不是表中。） 数据源，还有一个包含项名称“Paris”的非 OLAP 数据源。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th>属性</th>\n<th>值（OLAP 数据源）</th>\n<th>值（非 OLAP 数据源）</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>题注</strong></td>\n<td>Paris</td>\n<td>Paris</td>\n</tr>\n<tr>\n<td><strong>名称</strong></td>\n<td>[Europe].[France].[Paris]（只读）</td>\n<td>Paris</td>\n</tr>\n<tr>\n<td><strong>源名称</strong></td>\n<td>[Europe].[France].[Paris]（只读）</td>\n<td>（与 SQL 属性值相同，只读）</td>\n</tr>\n<tr>\n<td><strong>值</strong></td>\n<td>[Europe].[France].[Paris]（只读）</td>\n<td>Paris</td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>Name</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置一个 <strong>String</strong> 值，它代表对象的名称。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例在工作表 Sheet2 上新增查询表，并设置该查询表的名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\示例\\\\test1.accdb;DefaultDir=D:\\\\示例\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet2&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">Name</span> = <span class=\"hljs-string\">&quot;ExternalData_3&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B1 单元格的查询表的名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">`查询表的名称：<span class=\"hljs-subst\">${qyTable.Name}</span>`</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "Parent",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。  </p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>Parent</strong> 属性。</p>\n",
      "description": "<p>返回指定对象的父对象。只读。</p>\n",
      "examples": "",
      "type": "Object",
      "readonly": true
    },
    {
      "name": "PostText",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>PostText</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置用于 post 方法的字符串，post 方法用于向 Web 服务器输入数据以从 Web 查询中返回数据。<strong>String</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 A1 单元格的查询表用于 post 方法的字符串*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">PostText</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例设置第一张工作表上第一张查询表用于 post 方法的字符串*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">PostText</span> = <span class=\"hljs-string\">&quot;example1=a&amp;example2=b&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "PreserveColumnInfo",
      "summary": "<p>只有当查询表使用数据库连接时，该属性才有效。</p>\n<p>可以将该属性设置为 <strong>False</strong>，以便与 ET 的早期版本兼容。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>PreserveColumnInfo</strong> 属性。</p>\n",
      "description": "<p>如果每次刷新查询表时，列排序、筛选和布局信息都会保留，则该值为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一张工作簿上第一张工作表中创建新的查询表，并保留其列排序、筛选和布局信息。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\示例\\\\test1.accdb;DefaultDir=D:\\\\示例\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">Name</span> = <span class=\"hljs-string\">&quot;Contact List&quot;</span>\n    qyTable.<span class=\"hljs-property\">FieldNames</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">RowNumbers</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-property\">FillAdjacentFormulas</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">PreserveFormatting</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">RefreshOnFileOpen</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-property\">BackgroundQuery</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">RefreshStyle</span> = xlInsertDeleteCells\n    qyTable.<span class=\"hljs-property\">SavePassword</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">SaveData</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">AdjustColumnWidth</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">RefreshPeriod</span> = <span class=\"hljs-number\">0</span>\n    qyTable.<span class=\"hljs-property\">PreserveColumnInfo</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>(<span class=\"hljs-literal\">false</span>)\n\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示工作表 Sheet1 上包含 B1 单元格的查询表刷新时，是否保留列排序、筛选和布局信息。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Sheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">PreserveColumnInfo</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "PreserveFormatting",
      "summary": "<p>对于数据库查询表，默认的格式设置为 <strong>xlSimple</strong>。</p>\n<p>刷新查询表时，将对查询表应用新的自动套用格式样式。只要 <strong>PreserveFormatting</strong> 的值为 <strong>False</strong>，则 AutoFormat（自动套用格式）就会被设置为 <strong>None</strong>。这样，任何在 <strong>PreserveFormatting</strong> 被设置为 <strong>False</strong> 或在查询表刷新之前设置的自动套用格式都不会起作用，且相应产生的查询表也不会被应用任何格式。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>PreserveFormatting</strong> 属性。</p>\n",
      "description": "<p>如果将数据前五行的任何常用格式设置应用到查询表的新数据行，则为 <strong>True</strong>。对未使用的单元格不进行格式设置。如果将应用到查询表的最新一次自动套用格式应用于新数据行，则属性为 <strong>False</strong>。默认值是 <strong>True</strong>。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例将第一张工作表上的第一张查询表的PreserveFormatting属性设置为true。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">PreserveFormatting</span> = <span class=\"hljs-literal\">true</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例将第一张工作簿上第一张工作表中第一张查询表的 PreserveFormatting 属性设置为 false 。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">PreserveFormatting</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "QueryType",
      "summary": "<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlQueryType</strong> 可为以下这些 <strong>XlQueryType</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlTextImport</strong>。基于文本文件，仅用于查询表</td>\n</tr>\n<tr>\n<td><strong>xlOLEDBQuery</strong>。基于 OLE DB 查询，包括 OLAP（OLAP：为查询和报表（而不是处理事务）而进行了优化的数据库技术。OLAP 数据是按分级结构组织的，它存储在多维数据集而不是表中。） 数据源</td>\n</tr>\n<tr>\n<td><strong>xlWebQuery</strong>。基于网页，仅用于查询表</td>\n</tr>\n<tr>\n<td><strong>xlADORecordset</strong>。基于 ADO 记录集查询</td>\n</tr>\n<tr>\n<td><strong>xlDAORecordSet</strong>。基于 DAO 记录集查询，只用于查询表</td>\n</tr>\n<tr>\n<td><strong>xlODBCQuery</strong>。基于 ODBC 数据源</td>\n</tr>\n</tbody></table></div><p>在 <strong>Connection</strong>属性值的前缀中指定数据源。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>QueryType</strong> 属性。</p>\n",
      "description": "<p>表示 ET 填充查询表时所使用的查询类型。<strong>XlQueryType</strong> 类型，只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例判断如果第一张工作簿上第一张工作表上的第一个查询表的查询类型是基于网页，则刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qtQtrResults = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">if</span> (qtQtrResults.<span class=\"hljs-property\">QueryType</span> == xlWebQuery) {\n        qtQtrResults.<span class=\"hljs-title class_\">Refresh</span>()\n    }\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示工作表 Sheet2 上包含 B1 单元格的查询表的查询类型是否为 xlOLEDBQuery。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet2&quot;</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">QueryType</span> == xlOLEDBQuery)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlQueryType",
      "readonly": true
    },
    {
      "name": "RefreshOnFileOpen",
      "summary": "<p>在 Javascript 中使用 <strong>Open</strong> 方法打开工作簿时，不会自动刷新查询表和数据透视表。请在打开工作簿后，使用 <strong>Refresh</strong> 方法刷新数据。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>RefreshOnFileOpen</strong> 属性。</p>\n",
      "description": "<p>如果每次打开工作簿时，数据透视表高速缓存或查询表自动更新，则为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含单元格 B1 的查询表是否自动更新。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">RefreshOnFileOpen</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一张工作表上第一张查询表设置为自动更新，并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">RefreshOnFileOpen</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "RefreshPeriod",
      "summary": "<p>将周期设置为 0（零），则会禁用自动定时刷新，并且等同于将该属性设置为 <strong>Null</strong>。  </p>\n<p><strong>RefreshPeriod</strong> 属性的值可以是一个 0 到 32767 之间的整数。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>RefreshPeriod</strong> 属性。</p>\n",
      "description": "<p>返回或设置两次刷新之间的时间间隔。<strong>Long</strong> 型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一张工作表上第一张查询表两次刷新之间的时间间隔设置为 10 分钟，并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">RefreshPeriod</span> = <span class=\"hljs-number\">10</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含单元格 B1 的查询表两次刷���之间的时间间隔。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">RefreshPeriod</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "number（int4 精度）",
      "readonly": false
    },
    {
      "name": "RefreshStyle",
      "summary": "<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlCellInsertionMode</strong> 可为以下 <strong>XlCellInsertionMode</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlInsertDeleteCells</strong>插入或者删除部分行以适应新记录集所需要的实际行数。</td>\n</tr>\n<tr>\n<td><strong>xlOverwriteCells</strong>不向工作表添加新的单元格或行。如果溢出则覆盖周围单元格的数据。</td>\n</tr>\n<tr>\n<td><strong>xlInsertEntireRows</strong>必要时插入所有行以允许溢出。不从工作表删除单元格或行。</td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>RefreshStyle</strong> 属性。</p>\n",
      "description": "<p>返回或设置为了容纳查询返回的记录集中的行数而在指定工作表中插入或删除行时所使用的方式。<strong>XlCellInsertionMode</strong> 类型。可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例向 Sheet1 添加查询表，并对 RefreshStyle 属性进行设置，插入两个新行以放置查询结果数据。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Sheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;Finder;D:\\\\示例\\\\test6.dqy&quot;</span>, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;Sheet1!A1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">RefreshStyle</span> = xlInsertEntireRows\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第二张工作表上包含单元格 B1 的查询表 RefreshStyle 属性是否为 xlInsertDeleteCells。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">2</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">RefreshStyle</span> == xlInsertDeleteCells)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlCellInsertionMode",
      "readonly": false
    },
    {
      "name": "Refreshing",
      "summary": "<p>使用 <strong>CancelRefresh</strong> 方法可以取消后台查询。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>Refreshing</strong> 属性。</p>\n",
      "description": "<p>如果指定的查询表正在进行后台查询，则为 <strong>True</strong>。 <strong>Boolean</strong> 类型，只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一张工作表上查询表一正在进行后台查询的情况下显示了一条信息。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">if</span> (qyTable.<span class=\"hljs-property\">Refreshing</span>) {\n        <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">&quot;Query is currently refreshing: please wait&quot;</span>)\n    } <span class=\"hljs-keyword\">else</span> {\n        qyTable.<span class=\"hljs-title class_\">Refresh</span>(<span class=\"hljs-literal\">false</span>)\n        qyTable.<span class=\"hljs-property\">ResultRange</span>.<span class=\"hljs-title class_\">Select</span>()\n    }\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表是否正在进行后台查询。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">Refreshing</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": true
    },
    {
      "name": "ResultRange",
      "summary": "<p>该区域不包括字段名称行或行号列。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> �� <strong>QueryTable</strong> 属性访问 <strong>ResultRange</strong> 属性。</p>\n",
      "description": "<p>返回一个 <strong>Range</strong> 对象，该对象代表指定查询表所覆盖的工作表区域。只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对工作表 Sheet1 上查询表一中的第一列数据进行汇总，并在数据区域下方显示第一列数据的总和。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> range = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Sheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">ResultRange</span>.<span class=\"hljs-property\">Columns</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    range.<span class=\"hljs-property\">Name</span> = <span class=\"hljs-string\">&quot;Column1&quot;</span>\n    range.<span class=\"hljs-title class_\">End</span>(xlDown).<span class=\"hljs-title class_\">Offset</span>(<span class=\"hljs-number\">2</span>, <span class=\"hljs-number\">0</span>).<span class=\"hljs-property\">Formula</span> = <span class=\"hljs-string\">&quot;=sum(Column1)&quot;</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例选中第一张工作表上包含单元格 B1 的查询表的第二行。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    qyTable.<span class=\"hljs-property\">ResultRange</span>.<span class=\"hljs-property\">Rows</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">2</span>).<span class=\"hljs-title class_\">Select</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "Range",
      "readonly": true
    },
    {
      "name": "RobustConnect",
      "summary": "<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlRobustConnect</strong> 可为以下这些 <strong>XlRobustConnect</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlAlways</strong> 。缓存始终使用外部源信息（由 <strong>SourceConnectionFile</strong> 或 <strong>SourceDataFile</strong> 属性定义）进行重新连接。</td>\n</tr>\n<tr>\n<td><strong>xlAsRequired</strong> 。缓存通过 <strong>Connection</strong> 属性使用外部源信息进行重新连接。</td>\n</tr>\n<tr>\n<td><strong>xlNever</strong> 。缓存从不使用源信息进行重新连接。</td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>RobustConnect</strong> 属性。</p>\n",
      "description": "<p>返回或设置数据透视表缓存与其数据源连接的方式。<strong>XlRobustConnect</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含单元格 B1 的查询表连接到数据源的方式是否为 xlAsRequired。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">RobustConnect</span> == xlAsRequired)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例设置第一张工作表上第一张查询表连接到数据源的方式，并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">RobustConnect</span> = xlAlways\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlRobustConnect",
      "readonly": false
    },
    {
      "name": "RowNumbers",
      "summary": "<p>设置该属性为 <strong>True</strong> 并不会使行号立即显示。在查询表下一次刷新时行号将显示，而且在查询表每次刷新时将重新配置这些行号。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>RowNumbers</strong> 属性。</p>\n",
      "description": "<p>如果行号作为第一列添加到指定查询表，则该属性值为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例为第一张工作表上查询表“ExternalData_1”添加行号和字段名。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;ExternalData_1&quot;</span>)\n    qyTable.<span class=\"hljs-property\">RowNumbers</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">FieldNames</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含单元格 B1 的查询表是否在第一列展示行号。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">RowNumbers</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "SaveData",
      "summary": "<p>对于 OLAP（OLAP：为查询和报表（而不是处理事务）而进行了优化的数据库技术。OLAP 数据是按分级结构组织的，它存储在多维数据集而不是表中。） 数据源，此属性始终设置为 <strong>False</strong>。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>SaveData</strong> 属性。</p>\n",
      "description": "<p>如果将查询表的数据随工作簿一起保存，则为 <strong>True</strong>。如果仅保存查询表的定义，则为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上查询表“ExternalData_1”的数据是否随工作簿一起保存。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;ExternalData_1&quot;</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">SaveData</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一张工作表上第一张查询表设置为仅保存查询表的定义，并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">SaveData</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "SavePassword",
      "summary": "<p>此属性由数据透视表和查询表在 ODBC 和 OLEDB 查询中使用。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>SavePassword</strong> 属性。</p>\n<p>如果 <strong>ListObject</strong> 已连接到 SharePoint 列表，此属性将被忽略。</p>\n",
      "description": "<p>如果将 ODBC 连接字符串中的密码信息与指定查询一起保存，则为 <strong>True</strong>。如果不保存密码信息，则该值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例在每次保存第一张工作表上查询表一时，都不保存 ODBC 连接字符串的密码信息。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">SavePassword</span> = <span class=\"hljs-literal\">false</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含单元格 B1 的查询表是否保存 ODBC 连接字符串中的密码信息。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">SavePassword</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "SourceConnectionFile",
      "summary": "<p>来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>SourceConnectionFile</strong> 属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>SourceConnectionFile</strong> 属性。</p>\n",
      "description": "<p>返回或设置一个 <strong>String</strong> 值，它指明用于创建查询表的 ET 数据连接文件或类似文件。可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示创建第一张工作表上查询表“ExternalData_1”的数据连接文件。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;ExternalData_1&quot;</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">SourceConnectionFile</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例在第一张工作表上新增查询表，并设置其数据连接文件。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\示例\\\\test1.accdb;DefaultDir=D:\\\\示例\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">SourceConnectionFile</span> = <span class=\"hljs-string\">&quot;D:\\\\我的数据源\\\\test1.accdb student.odc&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "SourceDataFile",
      "summary": "<p>对于基于文件的数据源（如 Access），<strong>SourceDataFile</strong> 属性包含源数据文件的完全限定路径。对于基于服务器的数据源（如 SQL Server），此属性设置为 <strong>Null</strong>。如果通过编程方式更改 <strong>Connection</strong> 属性，则 <strong>SourceDataFile</strong> 属性设置为 <strong>Null</strong>。  </p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p>可以使用 <strong>ListObject</strong> 的 <strong>QueryTable</strong> 属性访问 <strong>SourceDataFile</strong> 属性。</p>\n",
      "description": "<p>返回或设置一个 <strong>String</strong> 值，它指明查询表的源数据文件。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示创建第一张工作表上查询表“ExternalData_1”的源数据文件。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;ExternalData_1&quot;</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">SourceDataFile</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例在第一张工作表上新增查询表，并设置其源数据文件。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\示例\\\\test1.accdb;DefaultDir=D:\\\\示例\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-property\">SourceDataFile</span> = <span class=\"hljs-string\">&quot;D:\\\\示例\\\\test1.accdb&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "TextFileColumnDataTypes",
      "summary": "<p>可以使用下表列出的 <strong>xlColumnDataType</strong>常量来指定在数据导入过程中所使用的列数据类型或执行的操作。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th>常量</th>\n<th>说明</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlGeneralFormat</strong></td>\n<td>概要</td>\n</tr>\n<tr>\n<td><strong>xlTextFormat</strong></td>\n<td>文本</td>\n</tr>\n<tr>\n<td><strong>xlSkipColumn</strong></td>\n<td>跳过列</td>\n</tr>\n<tr>\n<td><strong>xlDMYFormat</strong></td>\n<td>“日-月-年”日期格式</td>\n</tr>\n<tr>\n<td><strong>xlDYMFormat</strong></td>\n<td>“日-年-月”日期格式</td>\n</tr>\n<tr>\n<td><strong>xlEMDFormat</strong></td>\n<td>EMD 日期</td>\n</tr>\n<tr>\n<td><strong>xlMDYFormat</strong></td>\n<td>“月-日-年”日期格式</td>\n</tr>\n<tr>\n<td><strong>xlMYDFormat</strong></td>\n<td>“月-年-日”日期格式</td>\n</tr>\n<tr>\n<td><strong>xlYDMFormat</strong></td>\n<td>“年-日-月”日期格式</td>\n</tr>\n<tr>\n<td><strong>xlYMDFormat</strong></td>\n<td>“年-月-日”日期格式</td>\n</tr>\n</tbody></table></div><p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时，才使用此属性。</p>\n<p>如果在含有多列的数组中指定了多个元素，则将忽略那些值。</p>\n<p>只有在安装并选定了中国台湾地区语言时才可以使用 <strong>xlEMDFormat</strong>。<strong>xlEMDFormat</strong> 常量指定使用中国台湾地区纪元日期。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileColumnDataTypes</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置一个有序的常量数组，用于指定文本文件中相应列的数据类型，而该文本文件则是正要导入查询表中的文本文件。每一列的默认常量为 <strong>xlGeneral</strong>。<strong>Variant</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例向第一个工作簿中第一张工作表上的新查询表中导入一个固定宽度的文本文件。该文本文件的第一列为五个字符宽度，作为文本导入。第二列为四个字符宽度，被跳过。该文本文件的剩余部分被导入第三列，并对其应用常规格式。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlFixedWidth\n    qyTable.<span class=\"hljs-property\">TextFileFixedColumnWidths</span> = [<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">4</span>]\n    qyTable.<span class=\"hljs-property\">TextFileColumnDataTypes</span> = [xlTextFormat, xlSkipColumn, xlGeneralFormat]\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例设置活动工作表上新增查询表每一列的数据类型并刷新*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test3.txt&quot;</span>, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>))\n    qyTable.<span class=\"hljs-property\">TextFileColumnDataTypes</span> = [xlTextFormat, xlGeneralFormat, xlDMYFormat, xlGeneralFormat]\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "any",
      "readonly": false
    },
    {
      "name": "TextFileCommaDelimiter",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）并且 <strong>TextFileParseType</strong> 属性的值为 <strong>xlDelimited</strong> 时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong>导入。</p>\n<p><strong>TextFileCommaDelimiter</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果将文本文件导入查询表中时，以逗号作为分隔符，则该值为 <strong>True</strong>。如果以其他字符作为分隔符，则该值为 <strong>False</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上的查询表的分隔符设置为逗号，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileCommaDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上第一张查询表是否以逗号作为分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileCommaDelimiter</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "TextFileConsecutiveDelimiter",
      "summary": "<p>仅当查询表基于文本文件数据（<strong><a href=\"./apiObjectTemplate.htm?page=topics/WPS 基础接口/表格 API 参考/QueryTable/QueryTable .htm#QueryTable.QueryType\">QueryType</a></strong> 属性设置为 <strong>xlTextImport</strong>）并且 <strong><a href=\"./apiObjectTemplate.htm?page=topics/WPS 基础接口/表格 API 参考/QueryTable/QueryTable .htm#QueryTable.TextFileParseType\">TextFileParseType</a></strong> 属性的值为 <strong>xlDelimited</strong> 时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong><a href=\"./apiObjectTemplate.htm?page=topics/WPS 基础接口/表格 API 参考/QueryTable/QueryTable .htm#QueryTable.QueryTable\">QueryTable</a></strong> 对象导入，而所有其他外部数据将作为 <strong><a href=\"./apiObjectTemplate.htm?page=topics/WPS 基础接口/表格 API 参考/ListObject/ListObject .htm#ListObject.ListObject\">ListObject</a></strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileConsecutiveDelimiter</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>当向查询表中导入文本文件时，如果将连续分隔符看作是一个分隔符，则该值为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上新增查询表的分隔符设置为空格字符，然后刷新该查询表。连续的空格被当作单个分隔符处理。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">9</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileSpaceDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">TextFileConsecutiveDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 A1 单元格的查询表是否将连续分隔符看作是一个分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileConsecutiveDelimiter</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "TextFileDecimalSeparator",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）并且该文件包含的小数位分隔符和千位分隔符与计算机上使用的不同（由于使用不同的语言设置）时，才使用此属性。</p>\n<p>下表显示当使用不同分隔符向 ET 中导入文本时得到的不同结果。数字结果显示在最右边的列中。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th>系统小数分隔符</th>\n<th>系统千位分隔符</th>\n<th>TextFileDecimalSeparator 值</th>\n<th>TextFileThousandsSeparator 值</th>\n<th>导入的文本</th>\n<th>单元格的值（数据类型）</th>\n</tr>\n</thead><tbody><tr>\n<td>句号</td>\n<td>逗号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>123.123,45</td>\n<td>123,123.45（数字）</td>\n</tr>\n<tr>\n<td>句号</td>\n<td>逗号</td>\n<td>逗号</td>\n<td>逗号</td>\n<td>123.123,45</td>\n<td>123.123,45（文本）</td>\n</tr>\n<tr>\n<td>逗号</td>\n<td>句号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>123,123.45</td>\n<td>123,123.45（数字）</td>\n</tr>\n<tr>\n<td>句号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>逗号</td>\n<td>123 123.45</td>\n<td>123 123.45（文本）</td>\n</tr>\n<tr>\n<td>句号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>空格</td>\n<td>123 123.45</td>\n<td>123,123.45（数字）</td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileDecimalSeparator</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置小数分隔符，在将文本文件导入查询表中时，ET 将使用小数分隔符。默认值为系统小数分隔符。<strong>String</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例保存工作表 Sheet1 上第一张查询表最初的小数分隔符，并将其设置为逗号，以准备将一个法语文本文件（举例）导入 ET 的美国英语版中。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> strDecSep = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">TextFileDecimalSeparator</span>\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">TextFileDecimalSeparator</span> = <span class=\"hljs-string\">&quot;,&quot;</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表的小数分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileDecimalSeparator</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "TextFileFixedColumnWidths",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）并且 <strong>TextFileParseType</strong> 属性的值为 <strong>xlFixedWidth</strong> 时，才使用此属性。</p>\n<p>必须指定一个有效的非负值列宽。如果指定的列超出了文本文件的宽度，则会忽略那些值。如果文本文件的宽度大于指定的列的总宽度，则文本文件的剩余部分将会导入到一个附加列中。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileFixedColumnWidths</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置一个整数数组，该数组对应于正要向查询表中导入的文本文件的列宽（按字符）。有效的宽度为 1 到 32767 个字符。<strong>Variant</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例向第一个工作簿中第一张工作表上的新查询表中导入一个固定宽度的文本文件。该文本文件的第一列为五个字符宽度，作为文本导入。第二列为四个字符宽度，被跳过。该文本文件的剩余部分被导入第三列，并对其应用常规格式。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">9</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlFixedWidth\n    qyTable.<span class=\"hljs-property\">TextFileFixedColumnWidths</span> = [<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">4</span>]\n    qyTable.<span class=\"hljs-property\">TextFileColumnDataTypes</span> = [xlTextFormat, xlSkipColumn, xlGeneralFormat]\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上第一张查询表关联的文本文件的列宽（按字符）。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileFixedColumnWidths</span>.<span class=\"hljs-title function_\">toString</span>())\n}</code></pre></td></tr></tbody></table></div>",
      "type": "any",
      "readonly": false
    },
    {
      "name": "TextFileOtherDelimiter",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）并且 <strong>TextFileParseType</strong> 属性的值为 <strong>xlDelimited</strong> 时，才使用此属性。</p>\n<p>如果在字符串中指定了多个字符，则只使用第一个字符。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileOtherDelimiter</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置在向查询表中导入文本文件时用作分隔符的字符。默认值为 <strong>null</strong>。<strong>String</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上的查询表的分隔符设置为井号 (#)，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test4.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileOtherDelimiter</span> = <span class=\"hljs-string\">&quot;#&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表用作分隔符的字符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileOtherDelimiter</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "TextFileParseType",
      "summary": "<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlTextParsingType</strong> 可为以下 <strong>XlTextParsingType</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlFixedWidth</strong>表示将文件中的数据安排在固定宽度的列中。</td>\n</tr>\n<tr>\n<td><strong>xlDelimited</strong>默认值表示文件由分隔符分隔。</td>\n</tr>\n</tbody></table></div><p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileParseType</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置要导入查询表的文本文件中数据的列格式。<strong>XlTextParsingType</strong>类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例向第一个工作簿中第一张工作表上的新查询表中导入一个固定宽度的文本文件。该文本文件的第一列为五个字符宽度，作为文本导入。第二列为四个字符宽度，被跳过。该文本文件的剩余部分被导入第三列，并对其应用常规格式。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">9</span>, <span class=\"hljs-number\">1</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlFixedWidth\n    qyTable.<span class=\"hljs-property\">TextFileFixedColumnWidths</span> = [<span class=\"hljs-number\">5</span>, <span class=\"hljs-number\">4</span>]\n    qyTable.<span class=\"hljs-property\">TextFileColumnDataTypes</span> = [xlTextFormat, xlSkipColumn, xlGeneralFormat]\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表的列格式是否为 xlDelimited。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileParseType</span> == xlDelimited)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlTextParsingType",
      "readonly": false
    },
    {
      "name": "TextFilePlatform",
      "summary": "<p>默认值是在**“文本文件导入向导”<strong>的</strong>“文件原始格式”**选项中的当前设置。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlPlatform</strong> 可为以下 <strong>XlPlatform</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlMacintosh</strong></td>\n</tr>\n<tr>\n<td><strong>xlMSDOS</strong></td>\n</tr>\n<tr>\n<td><strong>xlWindows</strong></td>\n</tr>\n</tbody></table></div><p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时才使用该属性。</p>\n<p>如果使用用户界面导入数据，则会将来自 Web 查询或文本查询的数据作为 <strong>QueryTable</strong> 对象导入，而将所有其他外部数据作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，则必须将来自 Web 查询或文本查询的数据作为 <strong>QueryTable</strong> 导入，同时可将所有其他外部数据作为 <strong>ListObject</strong> 或 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFilePlatform</strong> 属性只应用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置正向查询表中导入的文本文件的原始格式。该属性确定在数据导入过程中使用何种代码页。<strong>XlPlatform</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例向第一个工作簿中第一张工作表上的查询表中导入一个 MS-DOS 文本文件，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">9</span>))\n    qyTable.<span class=\"hljs-property\">TextFilePlatform</span> = xlMSDOS\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileTabDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表的文本文件的来源是否为 xlWindows。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFilePlatform</span> == xlWindows)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "number（int4 精度）",
      "readonly": false
    },
    {
      "name": "TextFilePromptOnRefresh",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时才使用该属性。</p>\n<p>如果该属性值为 <strong>True</strong>，则第一次刷新查询表时，并不显示对话框。</p>\n<p>在用户界面中，其默认值为 <strong>True</strong>。</p>\n<p>如果使用用户界面导入数据，则会将来自 Web 查询或文本查询的数据作为 <strong>QueryTable</strong> 对象导入，而将所有其他外部数据作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，则必须将来自 Web 查询或文本查询的数据作为 <strong>QueryTable</strong> 导入，同时可将所有其他外部数据作为 <strong>ListObject</strong> 或 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFilePromptOnRefresh</strong> 属性只应用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果每次刷新查询表时都要指定导入文本文件的名称，则该属性值为 <strong>True</strong>。**“导入文本文件”**对话框允许用户指定路径和文件名。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在每次刷新第一个工作簿中第一张工作表上的查询表时，都提示用户输入文本文件的名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">9</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFilePromptOnRefresh</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-property\">TextFileTabDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表每次刷新时是否需要指定导入文本文件的名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFilePromptOnRefresh</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "TextFileSemicolonDelimiter",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileSemicolonDelimiter</strong>属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果在将文本文件导入查询表时使用分号作为分隔符，并且 <strong>TextFileParseType</strong> 属性的值为 <strong>xlDelimited</strong>，则为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上的查询表的分隔符设置为分号，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileSemicolonDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 B1 单元格的查询表是否将分号作为分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileSemicolonDelimiter</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "TextFileSpaceDelimiter",
      "summary": "<p>仅当查询表基于文本文件��据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）并且 <strong>TextFileParseType</strong> 属性的值为 <strong>xlDelimited</strong> 时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileSpaceDelimiter</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果向查询表中导入文本文件时，使用空格字符作为分隔符，则该值为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将空格字符设置为第一个工作簿中第一张工作表上的查询表的分隔符，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">7</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileSpaceDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上第一张查询表是否将空格字符作为分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileSpaceDelimiter</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "TextFileStartRow",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileStartRow</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置向查询表中导入文本文件时进行文本分列的起始行号。其有效值为 1 到 32767 之间的整数。默认值为 1。<strong>Long</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上查询表的文本分列起始行设置为第五行，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">8</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileStartRow</span> = <span class=\"hljs-number\">5</span>\n    qyTable.<span class=\"hljs-property\">TextFileTabDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上第一张查询表文本分列的起始行号。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileStartRow</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "number（int4 精度）",
      "readonly": false
    },
    {
      "name": "TextFileTabDelimiter",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）并且 <strong>TextFileParseType</strong> 属性的值为 <strong>xlDelimited</strong> 时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileTabDelimiter</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果向查询表中导入文本文件时使用 Tab 作为分隔符，则该值为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上的查询表的分隔符设置为 Tab，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">10</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileTabDelimiter</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 C1 单元格的查询表是否使用 Tab 作为分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;C1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileTabDelimiter</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "TextFileTextQualifier",
      "summary": "<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th><strong>XlTextQualifier</strong> 可为以下 <strong>XlTextQualifier</strong> 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlTextQualifierNone</strong></td>\n</tr>\n<tr>\n<td><strong>xlTextQualifierDoubleQuote</strong>默认值</td>\n</tr>\n<tr>\n<td><strong>xlTextQualifierSingleQuote</strong></td>\n</tr>\n</tbody></table></div><p>仅当查询表基于文本文件数据（<strong>QueryType</strong> 属性设置为 <strong>xlTextImport</strong>）时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileTextQualifier</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置向查询表中导入文本文件时的文本识别符。文本识别符用于指定包含的数据是文本格式。<strong>XlTextQualifier</strong>类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上的查询表的文本识别符设置为单引号。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(<span class=\"hljs-string\">&quot;TEXT;D:\\\\示例\\\\test.txt&quot;</span>, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">9</span>))\n    qyTable.<span class=\"hljs-property\">TextFileParseType</span> = xlDelimited\n    qyTable.<span class=\"hljs-property\">TextFileTextQualifier</span> = xlTextQualifierSingleQuote\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 B2 单元格的查询表的文本识别符是否为 xlTextQualifierNone。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B2&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileTextQualifier</span> == xlTextQualifierNone)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlTextQualifier",
      "readonly": false
    },
    {
      "name": "TextFileThousandsSeparator",
      "summary": "<p>仅当查询表基于文本文件数据（<strong>QueryTable</strong> 属性设置为 <strong>xlTextImport</strong>）时，尤其是当文件包含的小数位分隔符和千位分隔符与计算机上使用的不同（由于使用不同的语言设置）时，才使用此属性。</p>\n<p>下表显示当使用不同分隔符向 ET 中导入文本时得到的不同结果。数字结果显示在最右边的列中。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th>系统小数分隔符</th>\n<th>系统千位分隔符</th>\n<th>TextFileDecimalSeparator 值</th>\n<th>TextFileThousandsSeparator 值</th>\n<th>导入的文本</th>\n<th>单元格的值（数据类型）</th>\n</tr>\n</thead><tbody><tr>\n<td>句号</td>\n<td>逗号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>123.123,45</td>\n<td>123,123.45（数字）</td>\n</tr>\n<tr>\n<td>句号</td>\n<td>逗号</td>\n<td>逗号</td>\n<td>逗号</td>\n<td>123.123,45</td>\n<td>123.123,45（文本）</td>\n</tr>\n<tr>\n<td>逗号</td>\n<td>句号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>123,123.45</td>\n<td>123,123.45（数字）</td>\n</tr>\n<tr>\n<td>句号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>逗号</td>\n<td>123 123.45</td>\n<td>123 123.45（文本）</td>\n</tr>\n<tr>\n<td>句号</td>\n<td>逗号</td>\n<td>句号</td>\n<td>空格</td>\n<td>123 123.45</td>\n<td>123,123.45（数字）</td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileThousandsSeparator</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置向查询表中导入文本文件时 ET 所使用的千位分隔符。默认为系统千位分隔符。<strong>String</strong>类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例保存工作表 Sheet1 上第一个查询表最初的千位分隔符，并将其设置为句号，以准备将法语文本文件（举例）导入 ET 的美国英语版中。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> strDecSep = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">TextFileThousandsSeparator</span>\n    <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">TextFileThousandsSeparator</span> = <span class=\"hljs-string\">&quot;.&quot;</span>\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 C2 单元格的查询表的千位分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;C2&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">TextFileThousandsSeparator</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "TextFileTrailingMinusNumbers",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>TextFileTrailingMinusNumbers</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果为 <strong>True</strong>，ET 会将导入的数字视为以 - (减) 符号开头的文本作为负号。 如果为 <strong>False</strong>，则将导入的数字视为以 - 符号开头的文本作为文本。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例为 ET 确定包含单元格 B1 的查询表的设置，导入的数值对于以“-”符号开头的文本的处理方式。本示例假定在活动工作表上存在 QueryTable 对象。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-comment\">// Determine setting for TextFileTrailingMinusNumbers</span>\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>.<span class=\"hljs-property\">TextFileTrailingMinusNumbers</span>) {\n        <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">&quot;Numbers imported as text that begin with a &#x27;-&#x27; symbol will be treated as a negative symbol.&quot;</span>)\n    } <span class=\"hljs-keyword\">else</span> {\n        <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-string\">&quot;Numbers imported as text that begin with a &#x27;-&#x27; symbol will not be treated as a negative symbol.&quot;</span>)\n    }\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将活动工作表上包含 B3 单元格的查询表设置为数字开头的“-”符号按负号处理，并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B3&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    qyTable.<span class=\"hljs-property\">TextFileTrailingMinusNumbers</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "WebConsecutiveDelimitersAsOne",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性设置为 <strong>xlWebQuery</strong>，查询返回 HTML 文档，并且 <strong>WebPreFormattedTextToColumns</strong> 属性设置为 <strong>True</strong> 时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong>对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebConsecutiveDelimitersAsOne</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>当从网页的 HTML &lt;PRE&gt; 标记中向查询表导入数据时，如果将连续多个分隔符看作单个分隔符，并且数据将被分列，则该值为 <strong>True</strong>。如果将连续多个分隔符看作多个分隔符，则该值为 <strong>False</strong>。默认值是 <strong>True</strong>。可读/写 <strong>Boolean</strong> 类型。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例将第一个工作簿中第一张工作表上新增的查询表的分隔符设置为空格字符，然后刷新该查询表。连续多个空格作为单个空格处理。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">3</span>))\n    qyTable.<span class=\"hljs-property\">WebConsecutiveDelimitersAsOne</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上第一张查询表的连续分隔符是否被视为单个分隔符。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebConsecutiveDelimitersAsOne</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "WebDisableDateRecognition",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性设置为 <strong>xlWebQuery</strong> 并且查询返回 HTML 文档时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebDisableDateRecognition</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>向查询表中导入网页时，如果将类似日期的数据当作文本进行处理，则该值为 <strong>True</strong>。如果使用了日期识别，则该值为 <strong>False</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例关闭对第一张工作簿上第一张工作表中新增查询表的日期识别，以便类似于日期的网页数据将作为文本导入。然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">3</span>))\n    qyTable.<span class=\"hljs-property\">WebDisableDateRecognition</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上第一张查询表是否将类似日期的数据当作文本进行处理。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebDisableDateRecognition</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "WebDisableRedirections",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebDisableRedirections</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>如果对 <strong>QueryTable</strong> 对象禁用 Web 查询重定向，则该属性的值为 <strong>True</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例为 ET 确定工作簿中活动工作表的 Web 查询重定向的设置。本示例假定 QueryTable 对象位于活动工作表上，否则将出现运行错误。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-title function_\">alert</span>(<span class=\"hljs-title class_\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">WebDisableRedirections</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例开启对第一张工作表上第一张查询表的 Web 查询重定向。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">WebDisableRedirections</span> = <span class=\"hljs-literal\">false</span>\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "WebFormatting",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性设置为 <strong>xlWebQuery</strong> 并且查询返回 HTML 文档时，才使用此属性。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th>XlWebFormatting 可为以下 XlWebFormatting 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlWebFormattingAll</strong></td>\n</tr>\n<tr>\n<td><strong>xlWebFormattingRTF</strong></td>\n</tr>\n<tr>\n<td><strong>xlWebFormattingNone</strong>默认值</td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebFormatting</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置一个值，该值确定向查询表中导入网页时网页中应用了多少格式设置（如果有）。<strong>XlWebFormatting</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一个工作簿中第一张工作表上添加一个新的 Web 查询表，并导入应用于数据的所有网页格式，然后刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">5</span>))\n    qyTable.<span class=\"hljs-property\">WebFormatting</span> = xlWebFormattingAll\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上第一张查询表从网页应用的格式设置是否为xlWebFormattingNone。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebFormatting</span> == xlWebFormattingNone)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlWebFormatting",
      "readonly": false
    },
    {
      "name": "WebPreFormattedTextToColumns",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性为 <strong>xlWebQuery</strong> 并且查询返回 HTML 文档时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebPreFormattedTextToColumns</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置向查询表中导入网页时，是否对网页 HTML &lt;PRE&gt; 标记内的数据进行分列。默认值为 <strong>True</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一个工作簿中第一张工作表上添加一个新的 Web 查询表。注意，本示例并未对 HTML &lt;PRE&gt; 标记之间的数据进行分列。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">3</span>))\n    qyTable.<span class=\"hljs-property\">WebFormatting</span> = xlWebFormattingNone\n    qyTable.<span class=\"hljs-property\">WebPreFormattedTextToColumns</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示第一张工作表上包含 A1 单元格的查询表是否对网页 HTML &lt;PRE&gt; 标记内的数据进行分列。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebPreFormattedTextToColumns</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "WebSelectionType",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性设置为 <strong>xlWebQuery</strong> 并且查询返回 HTML 文档时，才使用此属性。</p>\n<p>如果此属性的值为 <strong>xlSpecifiedTables</strong>，则可以使用 <strong>WebTables</strong> 属性指定要导入的表格。</p>\n<div id=\"vstable\" style=\"font-family:&quot;white-space:normal;\"><table><thead><tr>\n<th>XlWebSelectionType 可为以下 XlWebSelectionType 常量之一。</th>\n</tr>\n</thead><tbody><tr>\n<td><strong>xlEntirePage</strong></td>\n</tr>\n<tr>\n<td><strong>xlAllTables</strong> 默认值</td>\n</tr>\n<tr>\n<td><strong>xlSpecifiedTables</strong></td>\n</tr>\n</tbody></table></div><p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebSelectionType</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回或设置一个值，该值决定是向查询表中导入整个网页、网页上的所有表格还是仅网页上的特定表格。<strong>XlWebSelectionType</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一个工作簿中第一张工作表上添加一个新的 Web 查询表，然后导入网页中第一个和第二个表格中的数据。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">6</span>))\n    qyTable.<span class=\"hljs-property\">WebFormatting</span> = xlWebFormattingNone\n    qyTable.<span class=\"hljs-property\">WebSelectionType</span> = xlSpecifiedTables\n    qyTable.<span class=\"hljs-property\">WebTables</span> = <span class=\"hljs-string\">&quot;1,2&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上包含 A4 单元格的查询表的 WebSelectionType 属性是否为 xlEntirePage。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;A4&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebSelectionType</span> == xlEntirePage)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "XlWebSelectionType",
      "readonly": false
    },
    {
      "name": "WebSingleBlockTextImport",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性设置为 <strong>xlWebQuery</strong> 并且查询返回 HTML 文档时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebSingleBlockTextImport</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>向查询表中导入网页时，如果位于指定网页的 HTML &lt;PRE&gt; 标记中的数据是同时进行处理的，则该值为 <strong>True</strong>。如果数据是以连续行的数据块方式导入的，以便能识别标题行，则该值为 <strong>False</strong>。默认值为 <strong>False</strong>。<strong>Boolean</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一个工作簿中第一张工作表上添加一个新的 Web 查询表，然后同时导入 HTML &lt;PRE&gt; 标记中的所有数据。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">5</span>))\n    qyTable.<span class=\"hljs-property\">WebSingleBlockTextImport</span> = <span class=\"hljs-literal\">true</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上第一个查询表是否同时进行处理网页上 HTML &lt;PRE&gt; 标记中的数据。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebSingleBlockTextImport</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "boolean",
      "readonly": false
    },
    {
      "name": "WebTables",
      "summary": "<p>仅当查询表的 <strong>QueryType</strong> 属性设置为 <strong>xlWebQuery</strong>，查询返回 HTML 文档，并且 <strong>WebSelectionType</strong> 属性的值为 <strong>xlSpecifiedTables</strong> 时，才使用此属性。</p>\n<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。</p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WebTables</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>向查询表中导入网页时，返回或设置由逗号分隔的表格名称或表格索引号的列表。<strong>String</strong> 类型，可读写。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例在第一个工作簿中第一张��作表上添加一个新的 Web 查询表，然后导入网页中第一个和第二个表格中的数据。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> sheet = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Workbooks</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;URL;https://bbs.wps.cn&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = sheet.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, sheet.<span class=\"hljs-property\">Cells</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1</span>))\n    qyTable.<span class=\"hljs-property\">WebFormatting</span> = xlWebFormattingNone\n    qyTable.<span class=\"hljs-property\">WebSelectionType</span> = xlSpecifiedTables\n    qyTable.<span class=\"hljs-property\">WebTables</span> = <span class=\"hljs-string\">&quot;1,2&quot;</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上第一个查询表由逗号分隔的表格索引号的列表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WebTables</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "type": "string",
      "readonly": false
    },
    {
      "name": "WorkbookConnection",
      "summary": "<p>如果使用用户界面导入数据，来自 Web 查询或文本查询的数据将作为 <strong>QueryTable</strong> 对象导入，而所有其他外部数据将作为 <strong>ListObject</strong> 对象导入。  </p>\n<p>如果使用对象模型导入数据，来自 Web 查询或文本查询的数据必须作为 <strong>QueryTable</strong> 导入，而所有其他外部数据既可作为 <strong>ListObject</strong> 导入，也可作为 <strong>QueryTable</strong> 导入。</p>\n<p><strong>WorkbookConnection</strong> 属性仅适用于 <strong>QueryTable</strong> 对象。</p>\n",
      "description": "<p>返回查询表所使用的 <strong>WorkbookConnection</strong> 对象。只读。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例显示活动工作表上第一个查询表连接到文本数据源的文本文件的名称。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-title function_\">alert</span>(qyTable.<span class=\"hljs-property\">WorkbookConnection</span>.<span class=\"hljs-property\">TextConnection</span>.<span class=\"hljs-property\">Connection</span>)\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例删除包含工作表 Sheet1 上第一个查询表的工作簿的外部数据连接。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-string\">&quot;Sheet1&quot;</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    qyTable.<span class=\"hljs-property\">WorkbookConnection</span>.<span class=\"hljs-title class_\">Delete</span>()\n}</code></pre></td></tr></tbody></table></div>",
      "type": "WorkbookConnection",
      "readonly": true
    }
  ],
  "functions": [
    {
      "name": "Delete",
      "summary": "",
      "description": "<p>删除对象。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例判断如果第一张工作表上第一张查询表的名称为“ExternalData_1”，则删除该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>)\n    <span class=\"hljs-keyword\">if</span> (qyTable.<span class=\"hljs-property\">Name</span> == <span class=\"hljs-string\">&quot;ExternalData_1&quot;</span>) {\n        qyTable.<span class=\"hljs-title class_\">Delete</span>()\n    }\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例删除第一张工作表上第一张查询表，然后显示该工作表上查询表的数量。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTables = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>\n    qyTables.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-title class_\">Delete</span>()\n    <span class=\"hljs-title function_\">alert</span>(qyTables.<span class=\"hljs-property\">Count</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "parameters": [],
      "returns": "void"
    },
    {
      "name": "Refresh",
      "summary": "<p>下列说明适用于基于 SQL 查询结果的 <strong>QueryTable</strong>对象。</p>\n<p><strong>Refresh</strong> 方法使 ET 连接到 <strong>QueryTable</strong> 对象的数据源，执行 SQL 查询，并将数据返回到基于 <strong>QueryTable</strong> 对象的区域。除非调用该方法，否则 <strong>QueryTable</strong> 对象不与数据源通信。</p>\n<p>当与 OLE DB 或 ODBC 数据源建立连接时，ET 使用由 <strong>Connection</strong> 属性指定的连接字符串。如果指定的连接字符串缺少必需的值，将显示对话框，提示用户提供必需的信息。如果 <strong>DisplayAlerts</strong> 属性为 <strong>False</strong>，则不显示对话框，<strong>Refresh</strong> 方法失败并导致“连接信息无效”异常。</p>\n<p>在 ET 建立一个成功的连接之后，将存储完整的连接字符串，这样，以后在同一编辑会话中调用 <strong>Refresh</strong> 方法时就不会再显示提示。通过检查 <strong>Connection</strong> 属性的值可以获得完整的连接字符串。</p>\n<p>完成数据库连接后，将检查 SQL 查询的有效性。如果该查询无效，<strong>Refresh</strong> 方法将失败并导致“SQL 语法错误”异常。</p>\n<p>如果查询需要参数，则必须在调用 <strong>Refresh</strong> 方法之前，用参数绑定信息初始化 <strong>Parameters</strong> 集合。如果未绑定足够的参数，<strong>Refresh</strong> 方法将失败并导致“参数错误”异常。如果将参数设置为提示用户输入参数值，则无论 <strong>DisplayAlerts</strong> 属性的设置如何，都会向用户显示对话框。如果用户取消参数对话框，<strong>Refresh</strong> 将停止并返回 <strong>False</strong>。如果对 <strong>Parameters</strong> 绑定了额外的参数，则这些额外参数将被忽略。</p>\n<p>如果成功地完成或启动查询，则 <strong>Refresh</strong> 方法返回 <strong>True</strong>；如果用户取消连接或参数对话框，该方法返回 <strong>False</strong>。</p>\n<p>要查看取回的行数是否超过了工作表中的可用行数，请检查 <strong>FetchedRowOverflow</strong> 属性。每次调用 <strong>Refresh</strong> 方法之前，该属性都将被初始化。</p>\n",
      "description": "<p>更新外部数据区域 (<strong>QueryTable</strong>)。</p>\n",
      "examples": "<div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*此示例在第一张工作表上添加一张查询表并刷新。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> connection = <span class=\"hljs-string\">&quot;ODBC;DSN=MS Access Database;DBQ=D:\\\\示例\\\\test1.accdb;DefaultDir=D:\\\\示例\\\\;DriverId=25;FIL=MS Access;MaxBufferSize=2048;PageTimeout=5;&quot;</span>\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">Worksheets</span>.<span class=\"hljs-title class_\">Item</span>(<span class=\"hljs-number\">1</span>).<span class=\"hljs-property\">QueryTables</span>.<span class=\"hljs-title class_\">Add</span>(connection, <span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;I1&quot;</span>), <span class=\"hljs-string\">&quot;SELECT * FROM `student`&quot;</span>)\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>()\n}</code></pre></td></tr></tbody></table></div><div id=\"vstable\"><table width=\"100%\"><tbody><tr><th>示例代码<span class=\"copy\" onclick=\"copyCode(this)\">复制</span></th></tr><tr><td><pre><code><span class=\"hljs-comment\">/*本示例对活动工作表上包含 B1 单元格的查询表进行设置，使表中不显示字段名称并刷新该查询表。*/</span>\n<span class=\"hljs-keyword\">function</span> <span class=\"hljs-title function_\">test</span>(<span class=\"hljs-params\"></span>) {\n    <span class=\"hljs-keyword\">let</span> qyTable = <span class=\"hljs-title class_\">Application</span>.<span class=\"hljs-property\">ActiveSheet</span>.<span class=\"hljs-title class_\">Range</span>(<span class=\"hljs-string\">&quot;B1&quot;</span>).<span class=\"hljs-property\">QueryTable</span>\n    qyTable.<span class=\"hljs-property\">FieldNames</span> = <span class=\"hljs-literal\">false</span>\n    qyTable.<span class=\"hljs-title class_\">Refresh</span>(<span class=\"hljs-literal\">false</span>)\n}</code></pre></td></tr></tbody></table></div>",
      "parameters": [
        {
          "name": "BackgroundQuery",
          "type": "any",
          "description": "<p>只用于基于 SQL 查询结果的 QueryTables。如果为 True，则在数据库建立连接并提交查询之后，将控制返回给过程。QueryTable 在后台进行更新。如果为 False，则在所有数据被取回到工作表之后，将控制返回给过程。如果没有指定该参数，则由 BackgroundQuery 属性的设置决定查询模式。</p>\n",
          "optional": true
        }
      ],
      "returns": "<p>Boolean</p>\n"
    }
  ]
}