{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "🟢 同步代码到 WPS (Trae)",
      "program": "${workspaceFolder}/dev/index_trae.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "outputCapture": "std",
      "env": {
        "NODE_OPTIONS": ""
      }
    }
  ]
}